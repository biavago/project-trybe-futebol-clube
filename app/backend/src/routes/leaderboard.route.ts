// import { Request, Router, Response } from 'express';
// // import LeaderboardController from '';

// //const teamController = new TeamController();

// const router = Router();

// router.get('/leaderboard', (req: Request, res: Response) => teamController.getAllTeams(req, res));
// router.get('/leaderboard/home', (req: Request, res: Response) => teamController.getAllTeams(req, res));
// router.get('/leaderboard/away', (req: Request, res: Response) => teamController.getTeamById(req, res));

// export default router;
