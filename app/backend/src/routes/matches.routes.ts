// import { Request, Router, Response } from 'express';
// import MatchesController from '../controllers/MatchesController';
// // import Validations from '../middlewares/Validations';

// const matchesController = new MatchesController();

// const router = Router();

// // router.post(
// //   '/',
// //   // Validations.validateBook,
// //   (req: Request, res: Response) => teamController.createTeam(req, res),
// // );

// router.get('/matches', (req: Request, res: Response) => matchesController.getAllMatches(req, res));
// router.patch('/matches/:id', (req: Request, res: Response) => matchesController.updateMatch(req, res));
// router.patch('/matches/:id/finish', (req: Request, res: Response) => matchesController.updateMatch(req, res));

// // router.put(
// //   '/:id',
// //   Validations.validateBook, // aproveitamos a validação já criada para o cadastro
// //   (req: Request, res: Response) =>
// //     bookController.updateBook(req, res),
// // );
// // router.delete(
// //   '/:id',
// //   (req: Request, res: Response) => bookController.deleteBook(req, res),
// // );

// export default router;
